# 🐧 mtemu (SimpOS v6) — Эмулятор Linux Терминала

**mtemu** — это не просто эмулятор терминала в браузере. Это полноценная среда с виртуальной файловой системой (VFS), менеджером пакетов и собственным скриптовым языком **SimpLang v6**, который позволяет создавать графические интерфейсы (GUI) прямо внутри консоли.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Version](https://img.shields.io/badge/version-6.3_Pro-green.svg)
![Platform](https://img.shields.io/badge/platform-Web-orange.svg)

---

## 🚀 Основные возможности

*   📂 **Виртуальная ФС:** Полноценная работа с файлами и папками (сохраняется в `localStorage`).
*   💻 **Bash-подобный интерфейс:** Привычные команды (`ls`, `cd`, `cat`, `rm` и др.).
*   🎨 **SimpLang v6:** Встроенный интерпретатор для запуска приложений с GUI.
*   📦 **Менеджер пакетов (`pkg`):** Установка софта из встроенного репозитория или по внешним ссылкам.
*   🖼️ **Оконная подсистема:** Запуск скриптов в модальных окнах или во весь экран.

---

## 🛠 Стандартные команды терминала

Эти команды работают как в обычном Linux:

| Команда | Описание | Пример |
| :--- | :--- | :--- |
| `ls [путь]` | Список файлов и папок | `ls /home/user` |
| `cd [путь]` | Смена текущей директории | `cd Documents` |
| `pwd` | Вывести путь текущей папки | `pwd` |
| `mkdir [имя]` | Создать новую папку | `mkdir my_project` |
| `touch [имя]` | Создать пустой файл | `touch notes.txt` |
| `cat [файл]` | Прочитать содержимое файла | `cat readme.txt` |
| `rm [-r] [путь]` | Удалить файл или папку (`-r` для папок) | `rm -r old_stuff` |
| `echo [текст]` | Вывести текст (поддерживает `>` и `>>`) | `echo "hi" > file.txt` |
| `cp [откуда] [куда]` | Копировать файл/папку | `cp file.txt backup.txt` |
| `mv [откуда] [куда]` | Переместить или переименовать | `mv old.txt new.txt` |
| `tree` | Показать дерево файлов | `tree /` |
| `neofetch` | Информация о системе | `neofetch` |
| `history` | История команд | `history` |
| `clear` | Очистить экран | `clear` |
| `reset` | Полный сброс системы и ФС | `reset` |

---

## 📦 Менеджер пакетов (`pkg`)

Система позволяет устанавливать дополнительные программы:

*   `pkg list` — список доступных для запуска программ в `/bin`.
*   `pkg install [name]` — установка программы из репозитория. По умолчанию доступны: `clicker`, `explorer`.
*   `pkg remove [name]` — удаление программы.

---

## 📜 Документация SimpLang v6

**SimpLang** — это скриптовый язык с объектно-ориентированным синтаксисом для создания приложений.

### Основы синтаксиса
*   `var x = 10` — создание переменной.
*   `label my_label` — создание метки для переходов.
*   `goto my_label` — переход к метке.
*   `if a == b goto label` — условие.
*   `stop` — завершение работы скрипта.

### Системные функции (`sys.*`)

| Функция | Параметры | Описание |
| :--- | :--- | :--- |
| `sys.window` | `title, width, height` | Создает окно приложения. Если не вызвать — будет Fullscreen. |
| `sys.notify` | `message, type` | Показать уведомление (`info`, `success`, `error`). |
| `sys.log` | `message` | Вывод в консоль разработчика браузера. |

### UI Функции (`ui.*`)

Используются для построения интерфейса. Все элементы привязываются к `parentId` (корневой элемент — `"root"`).

| Функция | Параметры | Описание |
| :--- | :--- | :--- |
| `ui.box` | `id, parentId` | Создает контейнер (div). |
| `ui.text` | `id, parentId, content` | Создает текстовый элемент. |
| `ui.button` | `id, parentId, text` | Создает кнопку. |
| `ui.input` | `id, parentId` | Создает поле ввода. |
| `ui.style` | `id, "css:rules;"` | Применяет стили (напр. `ui.style("my_box", "color: red; padding: 10px")`). |
| `ui.set` | `id, property, value` | Изменяет свойство элемента (напр. `ui.set("lbl", "text", "New value")`). |
| `ui.get` | `id, property, varName` | Записывает значение свойства в переменную скрипта. |
| `ui.bind` | `id, event, label` | Привязывает событие (`click`, `change`, `submit`) к метке в коде. |

### Работа с ФС в скриптах (`fs.*`)

| Функция | Параметры | Описание |
| :--- | :--- | :--- |
| `fs.scandir` | `path, outVar` | Сканирует папку и записывает список объектов в `outVar`. |
| `fs.mkdir` | `path` | Создает папку из скрипта. |

---

## 🎨 Пример приложения (Clicker)

```bash
# Файл: /bin/clicker.simp
sys.window("Clicker Game", "300px", "400px")
var score = 0

ui.box("main", "root")
ui.style("main", "display: flex; flex-direction: column; align-items: center; padding: 20px;")

ui.text("score_text", "main", "Score: 0")
ui.style("score_text", "font-size: 24px; margin-bottom: 20px;")

ui.button("btn", "main", "CLICK ME!")
ui.style("btn", "background: #4f46e5; color: white; padding: 10px 20px; border-radius: 8px;")
ui.bind("btn", "click", "on_click")

stop

label on_click
    var score = score + 1
    ui.set("score_text", "text", "Score: " + score)
    if score == 10 goto win
stop

label win
    sys.notify("You reached 10 points!", "success")
stop
```

---

## 🛠 Технологический стек

*   **React 19** — интерфейс терминала.
*   **Tailwind CSS** — стилизация.
*   **TypeScript** — типизация и логика.
*   **Local Storage API** — персистентность данных.

---

## 📝 Как запустить проект

1.  Склонируйте репозиторий.
2.  Откройте `index.html` в браузере (через Live Server или аналоги).
3.  Введите `help` для начала работы.

---
Разработано с ❤️ для любителей консоли.
